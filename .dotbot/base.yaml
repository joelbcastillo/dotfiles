- defaults:
    link:
      create: true
      relink: true
      force: true
    shell:
      stdout: true
      stderr: true

- clean: ['~']

- shell:
  -
    command: git submodule update --init --recursive
    description: Installing submodules
