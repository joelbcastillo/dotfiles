# This file is sourced when logging in interactively
# It runs after zshrc

# Print system information
# Only show neofetch for interactive local shells, not SSH
if command -v neofetch >/dev/null 2>&1 && [[ -z "$SSH_CLIENT" ]] && [[ -z "$SSH_TTY" ]] && [[ $- == *i* ]]; then
    neofetch
fi
